#include<iostream>
using namespace std;

int main() {
	string n;
	cin >> n;
	int l = n.length();
	int templ = l - 1;
	int c = 1;

	if (l == 0);

	else {
		for (int i = 0; i < l / 2; i++) {
			if (n[i] != n[templ ]) {
				c = 0;
				break;
			}
			templ--;
		}
	}
		cout << c;
}
